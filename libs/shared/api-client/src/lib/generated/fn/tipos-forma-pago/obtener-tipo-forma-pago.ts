/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { TipoFormaPago } from '../../models/tipo-forma-pago';

export interface ObtenerTipoFormaPago$Params {
  id: number;
}

export function obtenerTipoFormaPago(http: HttpClient, rootUrl: string, params: ObtenerTipoFormaPago$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: TipoFormaPago;
}>> {
  const rb = new RequestBuilder(rootUrl, obtenerTipoFormaPago.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: TipoFormaPago;
      }>;
    })
  );
}

obtenerTipoFormaPago.PATH = '/tipos-forma-pago/{id}';
