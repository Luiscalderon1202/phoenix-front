/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { TipoTelefono } from '../../models/tipo-telefono';
import { TipoTelefonoInput } from '../../models/tipo-telefono-input';

export interface ActualizarTipoTelefono$Params {
  id: number;

/**
 * Double-submit: debe coincidir con la cookie `csrf_token`. Obligatorio en
 * todo metodo que no sea GET, HEAD u OPTIONS.
 */
  'X-CSRF-Token': string;
      body: TipoTelefonoInput
}

export function actualizarTipoTelefono(http: HttpClient, rootUrl: string, params: ActualizarTipoTelefono$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: TipoTelefono;
}>> {
  const rb = new RequestBuilder(rootUrl, actualizarTipoTelefono.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-CSRF-Token', params['X-CSRF-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: TipoTelefono;
      }>;
    })
  );
}

actualizarTipoTelefono.PATH = '/tipos-telefono/{id}';
