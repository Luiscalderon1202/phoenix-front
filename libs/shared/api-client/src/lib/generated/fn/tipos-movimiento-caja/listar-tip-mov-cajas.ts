/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { TipMovCaja } from '../../models/tip-mov-caja';

export interface ListarTipMovCajas$Params {

/**
 * Busqueda parcial por nombre, sin distinguir mayusculas.
 */
  nombre?: string;

/**
 * I = ingreso, S = salida, A = ambos. Un valor fuera de esa lista se ignora en vez de vaciar el listado.
 */
  tipo?: 'I' | 'S' | 'A';

/**
 * Y = activos, N = inactivos. Vacio = todos.
 */
  estado?: 'Y' | 'N';
}

export function listarTipMovCajas(http: HttpClient, rootUrl: string, params?: ListarTipMovCajas$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: Array<TipMovCaja>;
}>> {
  const rb = new RequestBuilder(rootUrl, listarTipMovCajas.PATH, 'get');
  if (params) {
    rb.query('nombre', params.nombre, {});
    rb.query('tipo', params.tipo, {});
    rb.query('estado', params.estado, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: Array<TipMovCaja>;
      }>;
    })
  );
}

listarTipMovCajas.PATH = '/tipos-movimiento-caja';
