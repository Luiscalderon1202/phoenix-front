/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CondicionPago } from '../../models/condicion-pago';
import { CondicionPagoInput } from '../../models/condicion-pago-input';
import { Envelope } from '../../models/envelope';

export interface CrearCondicionPago$Params {

/**
 * Double-submit: debe coincidir con la cookie `csrf_token`. Obligatorio en
 * todo metodo que no sea GET, HEAD u OPTIONS.
 */
  'X-CSRF-Token': string;
      body: CondicionPagoInput
}

export function crearCondicionPago(http: HttpClient, rootUrl: string, params: CrearCondicionPago$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: CondicionPago;
}>> {
  const rb = new RequestBuilder(rootUrl, crearCondicionPago.PATH, 'post');
  if (params) {
    rb.header('X-CSRF-Token', params['X-CSRF-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: CondicionPago;
      }>;
    })
  );
}

crearCondicionPago.PATH = '/condiciones-pago';
