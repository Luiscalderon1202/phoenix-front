/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CondicionPago } from '../../models/condicion-pago';
import { Envelope } from '../../models/envelope';

export interface ObtenerCondicionPago$Params {
  id: number;
}

export function obtenerCondicionPago(http: HttpClient, rootUrl: string, params: ObtenerCondicionPago$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: CondicionPago;
}>> {
  const rb = new RequestBuilder(rootUrl, obtenerCondicionPago.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: CondicionPago;
      }>;
    })
  );
}

obtenerCondicionPago.PATH = '/condiciones-pago/{id}';
