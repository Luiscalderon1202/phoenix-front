/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AuthSessionPayload } from '../../models/auth-session-payload';
import { Envelope } from '../../models/envelope';

export interface AuthRefresh$Params {
}

export function authRefresh(http: HttpClient, rootUrl: string, params?: AuthRefresh$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: AuthSessionPayload;
}>> {
  const rb = new RequestBuilder(rootUrl, authRefresh.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: AuthSessionPayload;
      }>;
    })
  );
}

authRefresh.PATH = '/auth/refresh';
