/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';

export interface AuthEmpresas$Params {

/**
 * Acota al modulo indicado. Omitido o 0 devuelve las de cualquier modulo.
 */
  moduloid?: number;
}

export function authEmpresas(http: HttpClient, rootUrl: string, params?: AuthEmpresas$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: {
'empresas'?: Array<number>;
};
}>> {
  const rb = new RequestBuilder(rootUrl, authEmpresas.PATH, 'get');
  if (params) {
    rb.query('moduloid', params.moduloid, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: {
      'empresas'?: Array<number>;
      };
      }>;
    })
  );
}

authEmpresas.PATH = '/auth/empresas';
