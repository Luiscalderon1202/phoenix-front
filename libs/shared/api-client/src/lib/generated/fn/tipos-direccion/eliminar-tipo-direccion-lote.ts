/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { LoteIDs } from '../../models/lote-i-ds';
import { ResultadoLoteTipoDireccion } from '../../models/resultado-lote-tipo-direccion';

export interface EliminarTipoDireccionLote$Params {

/**
 * Double-submit: debe coincidir con la cookie `csrf_token`. Obligatorio en
 * todo metodo que no sea GET, HEAD u OPTIONS.
 */
  'X-CSRF-Token': string;
      body: LoteIDs
}

export function eliminarTipoDireccionLote(http: HttpClient, rootUrl: string, params: EliminarTipoDireccionLote$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: Array<ResultadoLoteTipoDireccion>;
}>> {
  const rb = new RequestBuilder(rootUrl, eliminarTipoDireccionLote.PATH, 'post');
  if (params) {
    rb.header('X-CSRF-Token', params['X-CSRF-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: Array<ResultadoLoteTipoDireccion>;
      }>;
    })
  );
}

eliminarTipoDireccionLote.PATH = '/tipos-direccion/lote/eliminar';
