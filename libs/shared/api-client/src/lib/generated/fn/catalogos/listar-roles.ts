/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { Rol } from '../../models/rol';

export interface ListarRoles$Params {
}

export function listarRoles(http: HttpClient, rootUrl: string, params?: ListarRoles$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: Array<Rol>;
}>> {
  const rb = new RequestBuilder(rootUrl, listarRoles.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: Array<Rol>;
      }>;
    })
  );
}

listarRoles.PATH = '/catalogos/roles';
