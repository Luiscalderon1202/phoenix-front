/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { PageMeta } from '../../models/page-meta';
import { PersonaListRow } from '../../models/persona-list-row';

export interface ListarPersonas$Params {

/**
 * Pagina, empezando en 1.
 */
  page?: number;

/**
 * Tamano de pagina. Se acota a 200 para que nadie pida la tabla entera.
 */
  page_size?: number;

/**
 * Busqueda global
 */
  q?: string;
  apemat?: string;
  nombre?: string;
  paisid?: number;
  sexo?: 'M' | 'F';
  estado?: 'Y' | 'N';
  con_foto?: 'Y' | 'N';

/**
 * N natural, J juridica
 */
  tipo?: 'N' | 'J';

/**
 * Repetible para filtrar por varios roles.
 */
  rolid?: Array<number>;
  orderby?: 'codigo' | 'nombre';
}

export function listarPersonas(http: HttpClient, rootUrl: string, params?: ListarPersonas$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: Array<PersonaListRow>;
'meta'?: PageMeta;
}>> {
  const rb = new RequestBuilder(rootUrl, listarPersonas.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('page_size', params.page_size, {});
    rb.query('q', params.q, {});
    rb.query('apemat', params.apemat, {});
    rb.query('nombre', params.nombre, {});
    rb.query('paisid', params.paisid, {});
    rb.query('sexo', params.sexo, {});
    rb.query('estado', params.estado, {});
    rb.query('con_foto', params.con_foto, {});
    rb.query('tipo', params.tipo, {});
    rb.query('rolid', params.rolid, {});
    rb.query('orderby', params.orderby, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: Array<PersonaListRow>;
      'meta'?: PageMeta;
      }>;
    })
  );
}

listarPersonas.PATH = '/personas';
