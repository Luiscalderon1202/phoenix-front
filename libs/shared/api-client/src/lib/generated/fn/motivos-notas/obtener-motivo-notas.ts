/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { MotivoNotas } from '../../models/motivo-notas';

export interface ObtenerMotivoNotas$Params {
  id: number;
}

export function obtenerMotivoNotas(http: HttpClient, rootUrl: string, params: ObtenerMotivoNotas$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: MotivoNotas;
}>> {
  const rb = new RequestBuilder(rootUrl, obtenerMotivoNotas.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: MotivoNotas;
      }>;
    })
  );
}

obtenerMotivoNotas.PATH = '/motivos-notas/{id}';
