/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { EstadoProcesoPedido } from '../../models/estado-proceso-pedido';
import { EstadoProcesoPedidoInput } from '../../models/estado-proceso-pedido-input';

export interface ActualizarEstadoProcesoPedido$Params {
  id: number;

/**
 * Double-submit: debe coincidir con la cookie `csrf_token`. Obligatorio en
 * todo metodo que no sea GET, HEAD u OPTIONS.
 */
  'X-CSRF-Token': string;
      body: EstadoProcesoPedidoInput
}

export function actualizarEstadoProcesoPedido(http: HttpClient, rootUrl: string, params: ActualizarEstadoProcesoPedido$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: EstadoProcesoPedido;
}>> {
  const rb = new RequestBuilder(rootUrl, actualizarEstadoProcesoPedido.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-CSRF-Token', params['X-CSRF-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: EstadoProcesoPedido;
      }>;
    })
  );
}

actualizarEstadoProcesoPedido.PATH = '/estados-proceso-pedido/{id}';
