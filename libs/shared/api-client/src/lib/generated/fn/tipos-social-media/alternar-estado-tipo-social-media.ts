/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { TipoSocialMediaEstado } from '../../models/tipo-social-media-estado';

export interface AlternarEstadoTipoSocialMedia$Params {
  id: number;

/**
 * Double-submit: debe coincidir con la cookie `csrf_token`. Obligatorio en
 * todo metodo que no sea GET, HEAD u OPTIONS.
 */
  'X-CSRF-Token': string;
}

export function alternarEstadoTipoSocialMedia(http: HttpClient, rootUrl: string, params: AlternarEstadoTipoSocialMedia$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: TipoSocialMediaEstado;
}>> {
  const rb = new RequestBuilder(rootUrl, alternarEstadoTipoSocialMedia.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-CSRF-Token', params['X-CSRF-Token'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: TipoSocialMediaEstado;
      }>;
    })
  );
}

alternarEstadoTipoSocialMedia.PATH = '/tipos-social-media/{id}/estado/alternar';
