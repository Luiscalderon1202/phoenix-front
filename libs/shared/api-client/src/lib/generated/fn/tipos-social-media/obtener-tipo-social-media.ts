/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Envelope } from '../../models/envelope';
import { TipoSocialMedia } from '../../models/tipo-social-media';

export interface ObtenerTipoSocialMedia$Params {
  id: number;
}

export function obtenerTipoSocialMedia(http: HttpClient, rootUrl: string, params: ObtenerTipoSocialMedia$Params, context?: HttpContext): Observable<StrictHttpResponse<Envelope & {
'data'?: TipoSocialMedia;
}>> {
  const rb = new RequestBuilder(rootUrl, obtenerTipoSocialMedia.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Envelope & {
      'data'?: TipoSocialMedia;
      }>;
    })
  );
}

obtenerTipoSocialMedia.PATH = '/tipos-social-media/{id}';
