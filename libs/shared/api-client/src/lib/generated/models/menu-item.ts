/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

export interface MenuItem {
  icono: string;
  menuid: number;

  /**
   * false = la pantalla sigue en el ERP legacy y exige navegacion completa
   * de pagina, no routerLink.
   */
  migrado: boolean;
  nivel: number;
  nombre: string;
  orden: number;

  /**
   * null en los nodos raiz.
   */
  parentId: (number | null);
  proceso?: string;

  /**
   * Ruta Angular si `migrado` es true; URL del `.php` legacy si es false.
   */
  ruta: string;
}
