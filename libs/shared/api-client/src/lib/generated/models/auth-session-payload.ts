/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { AuthUser } from '../models/auth-user';
import { Corporacion } from '../models/corporacion';
import { MenuItem } from '../models/menu-item';
import { MenuPermiso } from '../models/menu-permiso';

/**
 * Deja la sesion lista de un solo viaje. **No lleva tokens**: access y
 * refresh viajan en cookies httponly y el JS no los ve.
 */
export interface AuthSessionPayload {
  corporaciones: Array<Corporacion>;

  /**
   * La clave vigente es temporal. La temporal no es secreta (suele ser el
   * documento de la persona), asi que dejar operar sin cambiarla haria
   * inutil el resto.
   */
  debeCambiarClave?: boolean;

  /**
   * Vencimiento del access token.
   */
  expira_en?: string;
  menus: Array<MenuItem>;
  permissions: Array<MenuPermiso>;
  tenant: (Corporacion | null);
  user: AuthUser;
}
