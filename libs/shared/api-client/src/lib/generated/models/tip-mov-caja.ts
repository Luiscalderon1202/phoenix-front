/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */


/**
 * Fila del catalogo. Nunca hay nulls: el legacy usa cadena vacia.
 *
 * **No tiene `orden`**: es el unico catalogo del bloque sin esa columna.
 */
export interface TipMovCaja {

  /**
   * Etiqueta corta. Son 10 caracteres, no los 20 que anuncia el formulario del legacy.
   */
  abreviatura: string;

  /**
   * Se cambia por POST /{id}/estado/alternar.
   */
  estado: boolean;

  /**
   * Solo lectura desde esta API: el formulario del legacy la tiene comentada. Se conserva al editar.
   */
  estructura: string;
  nombre: string;

  /**
   * Cuenta del Plan Contable Empresarial. Solo lectura, como `estructura`.
   */
  pcgr_empresarial: string;

  /**
   * Cuenta del Plan Contable General. Solo lectura, como `estructura`.
   */
  pcgr_general: string;

  /**
   * Se cambia por POST /{id}/requerir-mes-anio/alternar.
   */
  requerir_mesanio: boolean;

  /**
   * Que hace falta para registrar el movimiento. El legacy ofrece un unico valor, SUBDPTO, mas la opcion vacia; el contrato no lo acota porque la columna no tiene check.
   */
  requiere: string;

  /**
   * I = ingreso, S = salida, A = ambos.
   */
  tipo: 'I' | 'S' | 'A';
  tipoid: number;
}
