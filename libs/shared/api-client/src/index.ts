// Cliente HTTP generado desde el contrato de phoenix-api.
//
// ⛔ NO editar nada bajo lib/generated: se regenera con `npm run api:client` a
// partir de ../phoenix-api/api/openapi.yaml. Cualquier cambio manual se pierde.
//
// Los dos repositorios estan separados, asi que el spec es el unico contrato:
// si el backend cambia una respuesta y no toca el spec, esto no se entera.
export * from './lib/generated';
